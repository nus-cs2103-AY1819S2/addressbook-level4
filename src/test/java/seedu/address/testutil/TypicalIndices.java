package seedu.address.testutil;

import seedu.address.commons.core.index.Index;

/**
 * A utility class containing a list of {@link Index} objects to be used in tests.
 */
public class TypicalIndices {
    public static final Index INDEX_FIRST_LESSON = Index.fromOneBased(1);
    public static final Index INDEX_SECOND_LESSON = Index.fromZeroBased(1);
}
