package seedu.address.logic.parser;

import org.junit.Test;

public class TagCommandParserTest {
    private AddCommandParser parser = new AddCommandParser();

    @Test
    public void parse_allFieldsPresent_success() {
        /*Pdf expectedPdf = new PdfBuilder(SAMPLE_PDF_1).withTags(TAG_VALID_LECTURE).build();

        // multiple tags - all accepted
        Pdf expectedPdfMultipleTags = new PdfBuilder(SAMPLE_PDF_1).withTags(TAG_VALID_CS2103T, TAG_VALID_LECTURE)
                .build();
        assertParseSuccess(parser, FILE_DESC_1_PDF + TAG_DESC_LECTURE + TAG_DESC_CS2103T,
                new TagCommand(expectedPdfMultipleTags));*/
    }

}
