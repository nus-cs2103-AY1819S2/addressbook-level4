package seedu.address.logic.commands;

//import static org.junit.Assert.assertEquals;

import org.junit.Test;

//import seedu.address.model.statistics.PlayerStatistics;
//import seedu.address.storage.JsonSerializableStatistics;

public class StatsStorageTest {


    @Test
    public void statsStorage_toModelType_success() {
        //JsonSerializableStatistics jsonStats = new JsonSerializableStatistics("0", "0", "0", "0", "0");
        //PlayerStatistics playerStats = new PlayerStatistics();
        //PlayerStatistics playerStatsB = jsonStats.toModelType();
        //assertEquals(playerStats, playerStatsB);
    }

}
